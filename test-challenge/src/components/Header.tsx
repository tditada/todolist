import React from 'react';

const HEADER_TEXT = 'To Do List With Advice';

const Header = () => {
    return (
        <header>_
            <h1>{HEADER_TEXT}</h1>
        </header>
    );
};

export default Header;